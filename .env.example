# Supabase Configuration
# ======================
# 
# Step 1: Go to https://supabase.com/dashboard and create a project
#
# Step 2: Enable Google OAuth:
#   - Authentication → Providers → Google → Enable
#   - Add your domain to "Authorized redirect URLs" if needed
#
# Step 3: Get your credentials:
#   - Project Settings → API → Project URL
#   - Project Settings → API → anon public (click "eye" to reveal)
#
# Step 4: Run the database setup:
#   - Copy the SQL from database/schema.sql
#   - Paste it into Supabase SQL Editor and run it

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
